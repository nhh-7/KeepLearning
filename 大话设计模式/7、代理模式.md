	代理模式（Proxy）：为其他对象提供一种代理以控制对这个对象的访问。
	Subject类：定义了 RealSubject和Proxy的共用接口，这样就在任何使用RealSubject的地方都可以使用Proxy。
	RealSubject类：继承Subject，定义Proxy所代表 的真实实体。
	Proxy类：继承Subject，保存一个RealSubject的引用使得代理可以访问实体，并提供一个与Subject的接口相同的接口这样代理就可以用来替代实体。