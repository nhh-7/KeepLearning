##### Shell函数
	格式：
		name(){
			commands
			return
		}
	其中name是函数名称，commands是函数包含的一系列命令。
	
	![[Pasted image 20241009212912.png]]
##### 局部变量
	local name
	在shell函数内，往变量名前加上local，从而定义了在该函数内部的局部变量，在函数外部，该变量不存在。
	局部变量允许程序员使用已经存在的变量名，无论这些变量名表示脚本中的全局变量，还是其他Shell函数中的变量，都不必担心命名冲突。